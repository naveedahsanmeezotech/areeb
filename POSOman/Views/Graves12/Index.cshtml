@model IEnumerable<MangoERP.Models.Grave>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Section_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grave_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grave_Status_Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Section_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grave_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grave_Status_Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
